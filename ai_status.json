{
  "last_updated": "2026-05-27",
  "last_session": "session 7b — Claude Code (main)",
  "git_sha": "0beb8db",
  "worktree_state": "clean on branch main — all changes committed and pushed. slack-bot/cloud-init/controller.yml remains locally dirty with live secrets; intentionally never committed.",
  "current_focus": "Session 7 wrapped up with a PARKING_LOT entry for Docker Compose auto-start on restore (cloud-init runcmd approach). All prior session work (bot fixes, dropdown pickers, /do-help, --auto-destroy) is merged to main.",
  "exact_next_step": "Apply the updated Slack app manifest to register /do-help: open https://api.slack.com/apps → DO Snap Bot → App Manifest → YAML tab → paste contents of slack-bot/manifest.yml → Save Changes.",
  "blockers": [
    {
      "description": "slack-bot/cloud-init/controller.yml contains a live 1Password service account token and SSH public key — must never be staged or committed. Always skip in git add.",
      "severity": "high",
      "owner": "operator"
    },
    {
      "description": "/do-help slash command added to manifest.yml but not yet re-applied at api.slack.com. Until the manifest is updated, /do-help will return 'dispatch_failed' in Slack.",
      "severity": "medium",
      "owner": "operator"
    }
  ],
  "open_tasks": [
    "Apply updated manifest.yml at api.slack.com to register /do-help (see exact_next_step above).",
    "Add auto-destroy support to the Slack bot's /do-restore command (analog of the --auto-destroy flag added to do-restore.sh) — not yet implemented in bot.py.",
    "Implement Docker Compose auto-start on restore via cloud-init runcmd (see docs/PARKING_LOT.md — operator should add restart: unless-stopped to compose files now; implementation deferred).",
    "Live-test /do-restore end-to-end with a real snapshot to confirm size-slug auto-selection and ssh-key attachment work correctly in production.",
    "Evaluate Oracle Cloud Always Free as zero-cost controller droplet replacement (see docs/slack-integration-options.md).",
    "Optionally configure scheduled snapshots and retention policy in .env.op on the controller (see docs/next-steps.md Part 4)."
  ],
  "completed_this_session": [
    "Added Docker Compose auto-start parking lot item to docs/PARKING_LOT.md with two-part note: operator action (restart: unless-stopped) and implementation spec (cloud-init runcmd via DOCKER_COMPOSE_DIRS env var + --compose-dir flag).",
    "Committed and pushed to main (0beb8db)."
  ]
}
