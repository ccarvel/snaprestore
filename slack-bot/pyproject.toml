[project]
name = "do-snap-bot"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "slack-bolt>=1.19",
    "httpx>=0.27",
    "aiohttp>=3.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
